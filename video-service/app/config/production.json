
{
  "server": {
    "port": {{SERVER_PORT}}
  },
  "uploadDir": "{{UPLOAD_DIR}}",
  "aws": {
    "s3": {
      "bucket": "{{S3_BUCKET}}"
    },
    "elasticTranscoder": {
      "pipelineId": "{{PIPELINE_ID}}"
    }
  }
}