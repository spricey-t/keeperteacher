{
  "server": {
    "port": {{SERVER_PORT}}
  },
  "identityservice": {
    "endpoint": "{{IDENTITY_SERVICE_ENDPOINT}}"
  },
  "drillservice": {
    "endpoint": "{{DRILL_SERVICE_ENDPOINT}}"
  },
  "videoservice": {
    "endpoint": "{{VIDEO_SERVICE_ENDPOINT}}"
  }
}
